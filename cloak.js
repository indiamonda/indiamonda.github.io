/* Compatibility stub for legacy sub-pages that hard-coded a reference
 * to /cloak.js. The actual title/icon swap lives in the shell, this
 * file just prevents the absolute-path include from 404'ing. */
;(function () {
  'use strict';
  if (window.__JqrgStubLoaded) return;
  window.__JqrgStubLoaded = true;
})();
